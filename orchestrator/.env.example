# Copy this file to .env and fill in values as needed.
# The orchestrator does not load .env automatically — source it yourself:
#   set -a && source .env && set +a

# --- Model tier selection ---
# Controls which model config.yaml key is used by main.py (and later the router).
# Values: local | remote | openrouter   (default: local)
ORCHESTRATOR_MODEL_TIER=local

# --- OpenRouter ---
# Required when ORCHESTRATOR_MODEL_TIER=openrouter.
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# --- Anthropic (direct) ---
# Required when ORCHESTRATOR_MODEL_TIER=remote.
ANTHROPIC_API_KEY=
